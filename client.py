import threading
import time
from random import randint
turno=False
def server():

def turno():
    """ Checa si es el turno del cliente """
def pedir():
    """ pide una carta al servidor """
def pasar():
    """ le dice al servidor que no quiere más cartas """
for num_hilo in range(3):
    server = threading.Thread(target=server)
    srever.start()